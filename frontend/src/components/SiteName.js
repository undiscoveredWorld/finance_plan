const SiteName = ({titleOnClick}) => {
    return (
        <div className="site-name clickable col-auto align-self-start m-0" onClick={titleOnClick}>
            Finance plan
        </div>
    )
}

export default SiteName